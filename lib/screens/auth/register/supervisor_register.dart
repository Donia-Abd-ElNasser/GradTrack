import 'package:flutter/widgets.dart';

class SupervisorRegister extends StatelessWidget {
  const SupervisorRegister({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}